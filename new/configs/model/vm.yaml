__target__: VelocityModule
frame_knn: 16
num_train_points: 128
feat_embedding_dim: 256
decoder_hidden_dim: 64
dsm_sigma: 0.01

# 推理时的 patch / Langevin 参数(原版硬编码在 predict_step 里, 这里提到 config 可调)
predict_patch_size: 1000
predict_seed_k: 6
predict_seed_k_alpha: 1
langevin_steps: 4
